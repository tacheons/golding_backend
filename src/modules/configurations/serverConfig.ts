import { url } from "inspector";

/**
 * @implementation :
 * Configuration file for the server
 *
 * @author
 * @since
 * v1.0.0
 */
let serverConfig = {
  port: 4000,
  url: "http://localhost",
  SECRET: "matrix512",
};

export default serverConfig;
